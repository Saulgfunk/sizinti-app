# Client-side (safe to expose — protected by RLS). Prefixed with EXPO_PUBLIC_
# so Expo inlines them into the app bundle. Fill in from your Supabase
# project's Settings > API page.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
